
const listeMot = ["Cachalot", "Petunia", "Serviette"]
let score = 0

let motUtilisateur = prompt("entre le mot : " + listeMot[0])

if (motUtilisater === listeMot[0]) {
    score++
}
console.log(score);


motUtilisateur = prompt("entre le mot : " + listeMot[1])

if (motUtilisater === listeMot[0]) {
    score++
}
console.log(score);

motUtilisateur = prompt("entre le mot : " + listeMot[2])

if (motUtilisater === listeMot[0]) {
    score++
}
console.log(score);




let prenom = "Jean";

const anneeDeNaissance = 1995;

let estMajeur = (2024 - anneeDeNaissance) >= 18;

let personne= {prenom: "Alice", age: 2024-anneeDeNaissance};

let couleurs = ["vert", "bleu", "violet"];

let x = 12;
let y = 7;

let resiltat = x + y; 
let estSuperieur = (x > y);
let condition = (x> 10 && y < 10);

let score = 72; 
if (score >= 80 ) {
    console.log ("Exellent");

} else if (score >= 50){
    console.log ("bien");
} else {
    console.log ("peut mieux faire");

}

function compterElements (tableau, callback)







